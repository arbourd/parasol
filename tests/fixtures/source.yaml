apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: test-chart
spec:
  chart:
    spec:
      chart: test-chart
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: test-repo
  values:
    replicaCount: 1
