{
  "enabledManagers": ["custom.regex", "flux", "github-actions", "helm-values"],
  "flux": {
    "fileMatch": ["^cluster/.+\\.yaml$"]
  },
  "helm-values": {
    "fileMatch": ["^cluster/.+/helmrelease\\.yaml$"]
  },
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["talconfig.yaml"],
      "matchStrings": [
        "kubernetesVersion: (?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "depNameTemplate": "kubernetes/kubernetes",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["talconfig.yaml"],
      "matchStrings": [
        "talosVersion: (?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "depNameTemplate": "siderolabs/talos",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    }
  ]
}
